[project]
name = "dynamic"
version = "0.1.0"
description = "dynamic using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.8.1"
]

[project.scripts]
kickoff = "dynamic.main:kickoff"
run_crew = "dynamic.main:kickoff"
plot = "dynamic.main:plot"
run_with_trigger = "dynamic.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
